# 아동 등록 앱 — 설치 안내서

프로그래밍을 몰라도 따라 할 수 있게 썼습니다.
**순서대로만 하시면 됩니다.** 중간을 건너뛰면 다음 단계가 막힙니다.

준비물은 구글 계정 하나와 PC 한 대입니다. 전체 1시간 정도 걸립니다.

전체 과정은 네 부분입니다.

| 부분 | 무엇을 하나 | 누가 | 시간 |
|---|---|---|---|
| 1부 | 본부 창고 만들기 (시트·폴더) | 본부 담당자 | 15분 |
| 2부 | 본부 접수처 만들기 (스크립트) | 본부 담당자 | 20분 |
| 3부 | 앱 올리기 (인터넷 주소 만들기) | 본부 담당자 | 20분 |
| 4부 | 직원 휴대폰에 설치 | 각 직원 | 3분 |

---

# 1부. 본부 창고 만들기

자료가 쌓일 곳을 먼저 만듭니다.

## 1-1. 구글 시트 만들기

1. 크롬 브라우저에서 `sheets.google.com` 에 접속합니다.
2. `+ 새 스프레드시트` 를 누릅니다.
3. 왼쪽 위 `제목 없는 스프레드시트` 를 눌러 이름을 **`아동등록DB`** 로 바꿉니다.

> **이미 아동등록DB를 만들어 쓰고 계셨다면** — 문항이 바뀌면서 시트 열도 바뀌었습니다.
> 시험 자료만 있는 상태라면 `아동마스터` 탭을 통째로 삭제하세요. 스크립트가 새 머리글로 다시 만듭니다.
> 실제 자료가 이미 쌓였다면 삭제하지 말고 알려주세요. 옮기는 방법을 따로 안내해 드리겠습니다.

탭(시트 아래쪽 이름표)은 만들 필요가 없습니다. 나중에 스크립트가 알아서 만듭니다.

## 1-2. 사진 폴더 만들기

1. `drive.google.com` 에 접속합니다.
2. 왼쪽 위 `+ 신규` → `새 폴더` → 이름을 **`아동사진`** 으로 만듭니다.
3. 만든 폴더를 **더블클릭해서 들어갑니다.**
4. 브라우저 주소창을 봅니다. 이렇게 생겼습니다.

```
https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz
                                        └──────── 이 부분이 폴더 ID ────────┘
```

5. `folders/` 뒤에 있는 긴 문자열을 **복사해서 메모장에 붙여넣어 두세요.**
   2부에서 씁니다.

## 1-3. 전송키 정하기

전송키는 **앱과 본부만 아는 비밀번호**입니다. 아무나 본부로 자료를 보내지 못하게 막습니다.

메모장에 아무 문자나 길게 적어두세요. 20자 이상이면 됩니다.

```
예시 : hawassa-2026-child-intake-9f3k2m
```

> 남에게 알려주지 마세요. 직원들에게는 앱에 미리 넣어둔 채로 배포하므로,
> 직원이 이 키를 알 필요도 없습니다.

**1부 끝. 메모장에 두 가지가 적혀 있어야 합니다 — 폴더 ID, 전송키.**

---

# 2부. 본부 접수처 만들기

직원 휴대폰이 보낸 자료를 받아서 시트에 적어 넣는 부분입니다.

## 2-1. 스크립트 편집기 열기

1. 1-1에서 만든 `아동등록DB` 시트를 엽니다.
2. 위쪽 메뉴에서 `확장 프로그램` → `Apps Script` 를 누릅니다.
3. 새 창이 열리고 `function myFunction() { }` 같은 글자가 보입니다.
4. **그 글자를 전부 지웁니다.** (`Ctrl+A` 로 전체 선택 후 `Delete`)

## 2-2. 코드 붙여넣기

1. 내려받은 폴더에서 `server` 폴더 안의 **`Code.gs`** 파일을 메모장으로 엽니다.
2. 전체 선택(`Ctrl+A`) 후 복사(`Ctrl+C`).
3. 아까 비워둔 스크립트 편집기에 붙여넣기(`Ctrl+V`).

## 2-3. 내 정보 채워 넣기

붙여넣은 코드의 **위쪽 15줄쯤**에 이렇게 생긴 부분이 있습니다.

```javascript
const CONFIG = {
  SHEET_ID:   '',
  MASTER_TAB: '아동마스터',
  PHOTO_TAB:  '사진',
  FOLDER_ID:  '여기에_드라이브_폴더_ID',
  KEY:        '여기에_전송키',
  SEQ_PAD:    4
};
```

**딱 두 줄만 고칩니다.** 따옴표 `'` 는 지우지 말고 그 안의 글자만 바꾸세요.

| 고칠 곳 | 무엇으로 | 주의 |
|---|---|---|
| `FOLDER_ID` | 1-2에서 복사한 폴더 ID | 따옴표 안에만 |
| `KEY` | 1-3에서 정한 전송키 | 따옴표 안에만 |

고친 뒤 모습입니다.

```javascript
  FOLDER_ID:  '1AbCdEfGhIjKlMnOpQrStUvWxYz',
  KEY:        'hawassa-2026-child-intake-9f3k2m',
```

`SHEET_ID` 는 **비워두세요.** 비워두면 지금 이 시트를 쓴다는 뜻입니다.

## 2-4. 저장하고 점검하기

1. 위쪽 **저장 아이콘(💾)** 을 누릅니다.
2. 상단 가운데 함수 선택 칸에서 **`설치점검`** 을 고릅니다.
3. **`▶ 실행`** 을 누릅니다.

### 처음 실행하면 권한을 물어봅니다

이 창들이 순서대로 나옵니다. 놀라지 마세요. **본인이 만든 스크립트라 정상 절차입니다.**

1. `권한 검토` → 누릅니다.
2. 본인 구글 계정을 고릅니다.
3. **`이 앱은 확인되지 않았습니다`** 경고가 뜹니다.
   → 왼쪽 아래 **`고급`** 을 누릅니다.
   → **`아동등록DB(안전하지 않음)으로 이동`** 을 누릅니다.
4. `허용` 을 누릅니다.

### 점검 결과 읽기

아래쪽 실행 로그에 이런 내용이 나오면 성공입니다.

```
연결된 시트 : 아동등록DB
아동마스터 탭 : 준비됨 (현재 0명)
사진 탭 : 준비됨 (현재 0장)
코드발급 탭 : 준비됨
사진 폴더 : 아동사진
전송키 : 설정됨 (32자)

→ 모두 정상입니다. 이제 [배포] 를 진행하세요.
```

`✗` 표시가 있으면 그 줄이 시키는 대로 고치고 다시 `실행` 하세요.

시트로 돌아가 보면 **탭 세 개가 자동으로 생겨 있습니다.**

## 2-5. 배포하기 (인터넷 주소 만들기)

여기가 가장 헷갈리는 부분입니다. 천천히 하세요.

1. 오른쪽 위 파란색 **`배포`** → **`새 배포`** 를 누릅니다.
2. 왼쪽 위 **톱니바퀴(⚙)** 를 누르고 **`웹 앱`** 을 고릅니다.
3. 아래 항목을 이렇게 맞춥니다.

| 항목 | 설정값 |
|---|---|
| 설명 | 아동등록 접수 (아무렇게나) |
| 다음 사용자 인증 정보로 실행 | **나** |
| 액세스 권한이 있는 사용자 | **모든 사용자** |

> **`모든 사용자` 가 아니면 앱에서 접속이 안 됩니다.**
> 걱정하실 필요는 없습니다. 전송키를 모르면 아무것도 할 수 없습니다.

4. **`배포`** 를 누릅니다.
5. **웹 앱 URL** 이 나타납니다. `/exec` 로 끝나는 긴 주소입니다.

```
https://script.google.com/macros/s/AKfycbx.....오래_긴_문자열...../exec
```

6. **`복사` 를 눌러 메모장에 붙여넣으세요.** 3부에서 씁니다.

## 2-6. 잘 되는지 확인

복사한 주소를 브라우저 새 탭에 붙여넣고 열어보세요.

> 아동등록 수신 서버가 동작 중입니다. 이 주소를 앱 설정의 [본부 서버 주소] 에 넣으세요.

이 문장이 보이면 성공입니다.

**2부 끝. 메모장에 세 가지가 적혀 있어야 합니다 — 폴더 ID, 전송키, 웹 앱 주소.**

---

# 3부. 앱 올리기

## 3-1. 앱에 본부 주소 미리 넣기 (권장)

이 단계를 하면 **직원들이 주소나 키를 입력할 필요가 없어집니다.** 실수도 사라집니다.

1. 내려받은 폴더에서 **`data.js`** 를 메모장으로 엽니다.
2. `Ctrl+F` 로 **`const SERVER`** 를 찾습니다. 위에서 30줄쯤에 있습니다.

```javascript
const SERVER={url:'',key:''};
```

3. 따옴표 안에 2부에서 받은 주소와 키를 넣습니다.

```javascript
const SERVER={url:'https://script.google.com/macros/s/AKfycbx...../exec',key:'hawassa-2026-child-intake-9f3k2m'};
```

4. 저장하고 닫습니다. **`저장할 때 인코딩은 UTF-8` 로 하세요.** (메모장: `다른 이름으로 저장` → 아래쪽 인코딩 칸)

## 3-2. 우리 사업장 목록으로 바꾸기

같은 `data.js` 파일 맨 위에 국가와 사업장 목록이 있습니다.

```javascript
const COUNTRIES=[
  {code:'ETH',ko:'에티오피아',en:'Ethiopia'},
  ...
const SITES=[
  {code:'HWS',country:'ETH',ko:'하와사',en:'Hawassa'},
  ...
```

**형식은 그대로 두고 안의 글자만 바꾸세요.**

- `code` : 아동코드에 들어갈 영문 약자 (3글자 권장)
- `country` : 그 사업장이 속한 국가의 `code`
- `ko` / `en` : 화면에 보일 이름

줄을 지우거나 추가할 때는 **줄 끝 쉼표(`,`)** 를 빠뜨리지 마세요. 마지막 줄만 쉼표가 없습니다.

## 3-3. GitHub에 올리기

1. `github.com` 에서 무료 계정을 만들고 로그인합니다.
2. 오른쪽 위 `+` → `New repository`.
3. `Repository name` 에 `child-intake` 를 넣고, **`Public`** 을 고른 뒤 `Create repository`.
4. 다음 화면에서 `uploading an existing file` 을 누릅니다.
5. 내려받은 폴더를 열고, **그 안의 파일 전부와 `icons`·`server` 폴더를 함께** 끌어다 놓습니다.

> **가장 흔한 실수** — 폴더째로 끌어다 놓는 것. 그러면 주소에 폴더 이름이 한 겹 더 붙어 앱이 열리지 않습니다.
> 올린 뒤 저장소 첫 화면에 **`index.html` 이 바로 보여야** 정상입니다.

6. 아래 `Commit changes` 를 누릅니다.

## 3-4. 주소 켜기

1. 저장소 위쪽 `Settings` → 왼쪽 메뉴 `Pages`.
2. `Source` 가 `Deploy from a branch` 인지 확인합니다.
3. `Branch` 를 **`main` / `(root)`** 으로 고르고 `Save`.
4. 1~2분 뒤 새로고침하면 주소가 나타납니다.

```
https://내계정이름.github.io/child-intake/
```

**이 주소가 직원들에게 보낼 앱 주소입니다.**

## 3-5. PC에서 먼저 시험

주소를 크롬에서 엽니다.

1. 담당자 이름·국가·사업장을 아무렇게나 넣습니다.
2. `본부 전송 주소` 칸이 **이미 채워져 있는지** 확인합니다. (3-1을 했다면 채워져 있습니다)
3. **`연결 확인`** 을 누릅니다. → **`본부와 연결됩니다.`** 가 나오면 성공입니다.
4. 아동을 한 명 끝까지 등록합니다. 사진은 아무 이미지 파일이나 고르면 됩니다.
5. 홈 화면에서 **`본부로 전송`** 을 누릅니다.
6. **구글 시트를 열어봅니다.** `아동마스터` 탭에 한 줄이 생기고 아동코드가 붙어 있어야 합니다.
7. 드라이브 `아동사진` 폴더에 사진이 들어와 있는지도 봅니다.

여기까지 되면 **전부 완성입니다.**

### 시험 자료 지우기

1. 시트에서 그 줄을 지웁니다.
2. 드라이브에서 사진을 지웁니다.
3. `코드발급` 탭의 `마지막연번` 을 `0` 으로 고칩니다.
4. 크롬에서 `F12` → `Application` → `Storage` → `Clear site data`.

> **주의** — 3번을 빠뜨리면 실제 등록이 0002부터 시작합니다.

---

# 4부. 직원 휴대폰에 설치

직원에게 주소만 보내면 됩니다. **인터넷이 되는 곳에서 한 번만** 하면 됩니다.

**안드로이드 (크롬)**
주소 접속 → 우측 상단 점 세 개 → `앱 설치` 또는 `홈 화면에 추가`

**아이폰 (사파리)**
주소 접속 → 아래쪽 공유 버튼 → `홈 화면에 추가`

> 아이폰은 **반드시 홈 화면에 추가해서 아이콘으로 실행**해야 합니다.
> 사파리 탭으로만 쓰면 저장된 자료가 삭제될 수 있습니다.

설치 후 첫 화면에서 **이름과 사업장만** 고르면 끝입니다.
이후로는 비행기 모드에서도 전부 동작합니다.

---

# 평소 운영

## 직원이 하는 일

1. 현장에서 아동을 등록합니다. 인터넷은 필요 없습니다.
2. 숙소나 사무실에서 인터넷이 잡히면 앱이 **알아서 전송합니다.**
3. 홈 화면 위쪽 경고 띠가 사라지면 다 보낸 것입니다.

수동으로 보내려면 `본부로 전송` 을 누르면 됩니다.

## 본부가 하는 일

`아동마스터` 탭의 **`상태`** 열을 봅니다.

| 상태 | 뜻 | 할 일 |
|---|---|---|
| `본부검수대기` | 정상 접수 | 내용 확인 후 처리 |
| `중복확인필요` | 같은 아동으로 의심됨 | **사진 두 장을 비교해 판단** |

`사진` 탭의 **`비고`** 열에는 자동 점검 결과가 적혀 있습니다.

- `자동점검 통과` — 문제없음
- `자동점검 주의: far` — 아동이 좀 멀리 찍힘
- `자동점검 경고: blur (담당자 확인 후 사용)` — 흔들렸지만 담당자가 확인 후 사용함

## 앱 내용을 고쳤을 때

**반드시 두 가지를 함께 하세요.**

1. GitHub에서 파일을 고칩니다. (파일명 클릭 → 연필 아이콘 → 고치고 `Commit changes`)
2. **`sw.js` 첫 줄의 버전 숫자를 올립니다.**

```javascript
const CACHE='child-intake-v3';   →   'child-intake-v4'
```

버전을 올리지 않으면 **이미 설치된 휴대폰은 옛 파일을 계속 씁니다.**

서버 스크립트(`Code.gs`)를 고쳤을 때는 Apps Script에서
`배포` → `배포 관리` → 연필 아이콘 → `버전: 새 버전` → `배포` 를 해야 반영됩니다.

---

# 관리자 페이지

홈 화면 아래 **`관리자 페이지`** 버튼으로 들어갑니다. 비밀번호가 필요합니다.

기본 비밀번호는 `data.js` 의 `const ADMIN={pin:'2026'}` 에 있습니다.
**배포 전에 바꾸시고**, 앱 안 `관리` 탭에서도 언제든 바꿀 수 있습니다.

## 국가·사업장 탭

목록의 오른쪽 버튼으로 **사용 / 사용 안 함** 을 바꿉니다.
꺼둔 항목은 직원의 설정 화면에서 보이지 않습니다.

- 국가를 끄면 **그 나라 사업장도 함께 꺼집니다.**
- `＋ 국가 추가` `＋ 사업장 추가` 로 새 항목을 넣을 수 있습니다. 코드는 영문 2~4글자입니다.
- 직접 추가한 항목만 삭제할 수 있고, **이미 등록에 쓰인 항목은 삭제되지 않습니다.**
  기본 목록은 삭제할 수 없습니다. `사용 안 함` 으로 두세요.

> 코드를 정할 때 주의하세요. 아동코드에 그대로 들어가므로 나중에 바꾸면 옛 자료와 어긋납니다.

## 문항은 고정입니다

등록 문항은 모두 필수이고 앱에서 켜고 끌 수 없습니다. 문항을 바꾸려면 `data.js` 와
`server/Code.gs` 의 열 목록, 그리고 구글 시트 머리글을 함께 고쳐야 합니다.

**등록 문항 (모두 필수)**

| 단계 | 문항 |
|---|---|
| 1 기본정보 | 아동 이름(현지어), 성별, 생년월일, 출생등록증 보유 |
| 2 아동사진 | 상반신 사진, 전신 사진(선택), 촬영 기준 확인 5개 |
| 3 가정환경 | 함께 사는 가족 수, 가족 구성 |
| 4 교육과 건강 | 학교에 다니나요, 학교명, 학년, 건강상태, 장애 유형, 신장, 체중 |
| 5 아동소개 | 장래희망, 좋아하는 과목, 좋아하는 색깔, 취미 |
| 6 동의 확인 | 프로필 미리보기, 동의서 촬영, 동의 3개 |

학교명과 학년은 `다니고있음` 을 골랐을 때만 나타납니다.
장애 유형은 건강상태에서 `장애` 를 골랐을 때만 나타나며, 여러 개를 함께 고를 수 있고
하나 이상 고르지 않으면 다음으로 넘어가지 않습니다.

## 관리 탭

- **관리자 비밀번호 변경** — 4자 이상
- **설정 파일 내보내기** — `아동등록앱_설정.json` 이 만들어집니다
- **설정 파일 불러오기** — 받은 파일을 고르면 그대로 적용됩니다
- **설정 초기화** — 처음 상태로. **등록된 아동 자료는 지워지지 않습니다.**

## 여러 기기에 같은 설정 적용하기

관리자 설정은 **그 기기에만** 저장됩니다. 다른 직원 폰에도 똑같이 적용하려면 둘 중 하나를 쓰세요.

1. **설정 파일 전달** — 본부에서 `설정 파일 내보내기` → 카톡이나 메일로 보내기 →
   직원이 `관리자 페이지 → 관리 → 설정 파일 불러오기`
2. **앱에 미리 넣어 배포 (권장)** — `data.js` 의 `COUNTRIES` `SITES` `TOGGLE_FIELDS` 를
   본부에서 고쳐 올리면, 새로 설치하는 모두에게 처음부터 적용됩니다.

정기적으로 바뀌지 않는 내용이라면 2번이 훨씬 간단합니다.

---

# 문제가 생겼을 때

| 증상 | 원인 | 해결 |
|---|---|---|
| 주소를 열면 404 | 폴더째로 올림 | 저장소 첫 화면에 `index.html` 이 보이도록 다시 올리기 |
| `웹 주소로 열어주세요` 화면 | 파일을 직접 열었음 | GitHub 주소로 접속 |
| `연결 확인` 실패 | 주소·키가 다름 | `/exec` 로 끝나는지, 키가 `Code.gs` 와 같은지 확인 |
| 전송했는데 시트가 비어 있음 | 배포를 안 했거나 옛 버전 | `배포 관리` 에서 새 버전으로 배포 |
| 사진만 안 올라감 | 폴더 ID 오류 | `설치점검` 다시 실행 |
| 등록 내용이 사라짐 | 아이폰 사파리 탭 사용 | 홈 화면 아이콘으로 실행 |
| 고친 내용이 반영 안 됨 | `sw.js` 버전 안 올림 | 버전 올리고 다시 커밋 |
| 아동코드가 건너뜀 | 시트 행을 지웠음 | 정상입니다. 번호는 되돌아가지 않습니다 |
| 사업장이 목록에 없음 | 관리자 페이지에서 꺼둠 | `관리자 페이지 → 국가·사업장` 에서 `사용` 으로 |
| 관리자 비밀번호를 잊음 | — | `data.js` 의 `ADMIN.pin` 확인, 또는 관리 탭에서 초기화 |

---

# 꼭 기억하실 것

1. **전송키는 외부에 알리지 마세요.** 저장소는 공개지만 아동 자료는 올라가지 않습니다. 단, `data.js` 에 키를 넣어두면 그 키는 공개됩니다. 조직 정책상 곤란하면 3-1을 건너뛰고 직원이 직접 입력하게 하세요.
2. **파일을 고치면 `sw.js` 버전을 올리세요.**
3. **아이폰은 홈 화면에 추가해서 쓰세요.**
4. **전송하지 않은 등록은 그 휴대폰에만 있습니다.** 하루 일과가 끝나면 전송하는 것을 원칙으로 삼으세요.
5. **개인정보 국외 이전과 보호자 동의서 문구는 파일럿 전에 법무 확인을 받으세요.**
