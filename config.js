/* ══════════════════════════════════════════════════════════════
   우리 조직 설정 — 앱을 새 버전으로 바꿀 때 이 파일은 그대로 두세요.
   여기 말고 다른 파일은 손댈 일이 없습니다.
   ══════════════════════════════════════════════════════════════ */

/* ── 국가 목록 ──
   code : 아동코드에 들어갈 영문 약자 (2~4글자)
   줄 끝 쉼표에 주의하세요. 마지막 줄만 쉼표가 없습니다. */
const COUNTRIES=[
  {code:'VNM',ko:'베트남',en:'Vietnam'}, {code:'NPL',ko:'네팔',en:'Nepal'}];

/* ── 사업장 목록 ──
   country : 위 COUNTRIES 의 code 와 정확히 같아야 합니다. */
const SITES=[
  {code:'TAS',country:'VNM',ko:'떤선',en:'Tan Son'},
  {code:'DTI',country:'NPL',ko:'다딩',en:'Dhading'}];

/* ── 본부 수신 서버 ──
   배포한 Apps Script 웹앱 주소와 전송키.
   저장소가 공개(Public)라면 반드시 비워두세요. 키가 인터넷에 노출됩니다.
   비워두면 직원이 앱 설정 화면에서 직접 넣거나 QR 링크로 받습니다. */
const SERVER={url:'',key:''};

/* ── 관리자 페이지 비밀번호 ──
   배포 전에 바꾸세요. 앱 안에서도 바꿀 수 있습니다. */
const ADMIN={pin:'2026'};
